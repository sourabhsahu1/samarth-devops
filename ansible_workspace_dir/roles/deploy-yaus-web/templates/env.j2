# Remove sample from the filename and add your environment file variables here
# This file is generally used to refer in docker-compose file

POSTGRES_DB={{ credentials.POSTGRES_DB }}     # from separately hosted PSQL service
POSTGRES_USER={{ credentials.POSTGRES_USER }}   # from separately hosted PSQL service
POSTGRES_PASSWORD={{ credentials.POSTGRES_PASSWORD }}   # from separately hosted PSQL service
REDIS_URI={{ credentials.REDIS_URI }}   # from separately hosted Redis service
REDIS_NAME={{ credentials.REDIS_NAME }} # from separately hosted Redis service
GRAPHQL_URI=http://{{ service_yaus_hasura_service }}:{{ service_yaus_hasura_host_port }}/v1/graphql   # yaus-hasura URL
HASURA_ADMIN_SECRET={{ credentials.HASURA_ADMIN_SECRET }}   # must be same as ansible_workspace_dir/roles/deploy-yaus-hasura/vars/main.yml::HASURA_GRAPHQL_ADMIN_SECRET
BASE_URL={{ credentials.BASE_URL }}
RABBITMQ_PASSWORD={{ credentials.RABBITMQ_PASSWORD }}  # must be same as ansible_workspace_dir/roles/deploy-yaus-broker/vars/main.yml::RABBITMQ_PASSWORD
RABBITMQ_USERNAME={{ credentials.RABBITMQ_USERNAME }}  # must be same as ansible_workspace_dir/roles/deploy-yaus-broker/vars/main.yml::RABBITMQ_USERNAME
RMQ_URL=amqp://${RABBITMQ_USERNAME}:${RABBITMQ_PASSWORD}@{{ service_yaus_broker }}:{{ service_yaus_broker_docker_port }}
RMQ_QUEUE={{ credentials.RMQ_QUEUE }}
RMQ_QUEUE_DURABLE={{ credentials.RMQ_QUEUE_DURABLE }}
POSTHOG_API_KEY={{ credentials.POSTHOG_API_KEY }}
POSTHOG_API_HOST={{ credentials.POSTHOG_API_HOST }}
POSTHOG_DISTINCT_KEY={{ credentials.POSTHOG_DISTINCT_KEY }}
POSTHOG_BATCH_SIZE={{ credentials.POSTHOG_BATCH_SIZE }}
POSTHOG_FLUSH_INTERVAL={{ credentials.POSTHOG_FLUSH_INTERVAL }}
CLICK_BACKUP_CRON={{ credentials.CLICK_BACKUP_CRON }}

# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB (Preview).
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL={{ credentials.DATABASE_URL }}   # as provided as a separate hosted PSQL URL